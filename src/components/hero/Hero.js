import React from 'react';

const Hero = () => {
    return (
        <div id="hero">
            <div className="container">
                <div className="hero">
                    <h1>hero block</h1>
                </div>
            </div>
        </div>
    );
};

export default Hero;